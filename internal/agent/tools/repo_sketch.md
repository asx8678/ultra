Build a token-bounded, production-first semantic map of the current repository.

Use this before broad exploration in a large or unfamiliar repository. It indexes supported source and configuration files, then returns important files, symbols, routes, language coverage, and exact suggested read windows. Tests and generated files are de-emphasized or skipped. This complements exact grep, view, and LSP tools; it does not replace them.
